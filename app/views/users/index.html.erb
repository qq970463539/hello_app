<% provide(:title,"All users") %>
<h1>All users</h1>

<div class="row justify-content-center">
    <div class="col-md-8">

        <%= will_paginate @users %>

        <ul class="users">
            <%= render @users %>
        </ul>

        <%= will_paginate @users %>
    </div>
</div>